export enum ProductSortType {
  RATING = 'rating',
  PRICE = 'price',
  DATE = 'date',
  TOTALORDERS = 'total_orders',
}

export enum LoadingStatus {
  Idle = 'idle',
  Loading = 'loading',
  Success = 'success',
  Failed = 'failed',
}
