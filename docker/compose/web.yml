version: "3.9"

services:
  web:
    container_name: web
    build:
      context: ${PWD}
      dockerfile: docker/Dockerfile
    command: /bin/bash -c \
      "echo '[+] Run frontend' && \
      yarn start"
    expose:
      - ${EXPOSE_PORT}
    env_file:
      - ${PWD}/.env
    restart: unless-stopped
